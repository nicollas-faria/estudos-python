x = 3.14
y = 4
z = 5

# result = round(x) # arredondar numero
# result = abs(y) # Retorna o valor absoluto(A sua distancia em relação ao zero)
# result = pow(4, 3) # Calcula a potencia de um numero
# result = max(x, y ,z) # entrega o valor maximo(mais alto) entre as 3 opções
result = min(x, y, z) # entrega o valor minimo(mais baixo) entre as 3 opções

print(result)